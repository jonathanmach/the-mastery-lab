import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'vue.electron.capacitor.experiment',
  appName: 'vue-electron-capacitor-experiment',
  webDir: 'dist'
};

export default config;
